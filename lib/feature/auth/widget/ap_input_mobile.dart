import 'package:flutter/material.dart';

class ApInputMobile extends StatelessWidget {
  const ApInputMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
